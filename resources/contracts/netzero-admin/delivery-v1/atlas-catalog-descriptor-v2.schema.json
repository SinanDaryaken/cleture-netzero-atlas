{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:netzeroadmin:schema:atlas-catalog-descriptor:2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "owner",
    "catalog",
    "version",
    "content_schema_version",
    "canonicalization",
    "sha256",
    "size_bytes",
    "artifact_path"
  ],
  "properties": {
    "schema_version": {
      "enum": [
        "atlas-catalog-snapshot/v1",
        "atlas-catalog-snapshot/v2"
      ]
    },
    "owner": {
      "const": "NetZeroAdmin"
    },
    "catalog": {
      "enum": [
        "unit",
        "geography"
      ]
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1000
    },
    "content_schema_version": {
      "enum": [
        "unit-catalog-release/v2",
        "netzero-geography-snapshot/v1"
      ]
    },
    "canonicalization": {
      "const": "netzero-sorted-json-v1"
    },
    "sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "size_bytes": {
      "type": "integer",
      "minimum": 1
    },
    "artifact_path": {
      "type": "string",
      "pattern": "^atlas-catalog-snapshots/(unit|geography)/sha256/[a-f0-9]{64}\\.json$"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "catalog": {
            "const": "unit"
          }
        },
        "required": [
          "catalog"
        ]
      },
      "then": {
        "properties": {
          "content_schema_version": {
            "const": "unit-catalog-release/v2"
          },
          "artifact_path": {
            "pattern": "^atlas-catalog-snapshots/unit/sha256/"
          },
          "schema_version": {
            "const": "atlas-catalog-snapshot/v2"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "catalog": {
            "const": "geography"
          }
        },
        "required": [
          "catalog"
        ]
      },
      "then": {
        "properties": {
          "content_schema_version": {
            "const": "netzero-geography-snapshot/v1"
          },
          "artifact_path": {
            "pattern": "^atlas-catalog-snapshots/geography/sha256/"
          },
          "version": {
            "pattern": "^sha256:[a-f0-9]{64}$"
          },
          "schema_version": {
            "const": "atlas-catalog-snapshot/v1"
          }
        }
      }
    }
  ],
  "description": "Catalog bundle 2.0.0: unit uses descriptor v2 and content v2; geography retains descriptor v1 and content v1 byte identities. Historical unit v1 uses catalog bundle 1.0.0."
}
