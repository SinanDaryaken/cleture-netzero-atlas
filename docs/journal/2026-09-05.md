# 2026-09-05 — Bağımsız validation ve candidate build fazı

## Kullanıcı kapsamı

Bağımsız Atlas işleri geliştirilecek, testler yazılıp çalıştırılacak. Başka proje
sorumlulukları önce sahiplerine iletilecek ancak yalnız BEKLEYEN günlük kaydı olarak
tutulacak; geliştirmeye başlanmayacak. Devam noktası kalıcı kaydedilecek.

Admin “Durumu analiz et” task'ına ve Worker “Bul kaldığımız yeri” task'ına kayıt-only
bildirim yapıldı. Admin kayıt teyidi verdi; Worker inbox girdisi doğrudan okuma ile
doğrulandı. Güncel yollar [CURRENT](../CURRENT.md) içinde. Bu bildirimler rollout yetkisi değildir.

## Uygulananlar

- CandidateFinding ile package/field/evidence bağlamı; schema yanında semantic validation.
- Hash-pinned validation ve mapping policy, V1'i değiştirmeyen diff V2.
- Source/release'e bağlı exact descriptor/terms lisans kanıtı okuyucusu.
- Artifact/kanıt/policy kimlikli validation receipt ve immutable findings ledger.
- Receipt zorunlu package builder ve ZIP öncesi member hash/size yeniden doğrulaması.
- Kısa release-lock transaction ile retry boyunca aynı UUID/run/timestamp.
- Ortak ZIP byte'larına sahip farklı manifestler için archive uniqueness düzeltmesi.
- Verified previous manifest/ZIP/entity okuyucusu; payload'ları disk SQLite indeksine
  alan source-neutral release diff; 10 MiB entity satır sınırı.
- `atlas:candidate:build` sabit use-case ve açık build planı; otomatik gönderim yok.
- README, roadmap, ADEME inventory, işletim notu, ADR-007 ve devam günlüğü.

Laravel best-practices/testing kuralları doğrultusunda domain/adapter ayrımı, explicit
configuration, fail-closed kanıt kontrolleri ve izole test verisi korundu. Başlangıçta
memory/code-graph araçları sunulmadığından recall/index doğrulaması yapılamadı; doğrudan
kaynak okuma kullanıldı. Bu kalıcı repo günlüğü sonraki oturumun devam kaynağıdır.

## Doğrulama kanıtları

- PHP 8.5.8, Composer 2.10.2, Laravel 13.30.1, Boost 2.7.0.
- Son Unit/Feature: **91 test, 344 assertion**, SQLite memory, 20:07.
  `C:\Users\Sinan\AppData\Local\Temp\atlas-full-final-20260905-200708.log`
- Son gerçek PostgreSQL/MinIO: **1 test, 10 assertion**, iki bağımsız PHP süreci;
  eşzamanlı build/retry, unchanged previous diff ve bozuk archive reddi.
  `C:\Users\Sinan\AppData\Local\Temp\atlas-postgres-minio-final-20260905-200711.log`
- Yeni lisans metadata'sı → aynı archive / farklı package+manifest testi tam suite içindedir.
- İlk dar test çalıştırmalarında fixture locator tipi, empty-object temsili ve fake
  storage dosya izinleri düzeltildi. İlk concurrency harness'teki fork, HTTP client
  bağlantı durumunu miras alıyordu; iki bağımsız process ile gerçek worker sınırı kuruldu.
  Yukarıdaki son yeşil sonuçlar önceki başarısız denemelerin yerini alır.
- Pint ve `composer validate --strict` geçti; `git diff --check` temiz.
- Yeni migration hedef DB read-only doğrulandıktan sonra exact path ile yalnız
  `atlas_netzero` üzerinde uygulandı. PostgreSQL test DB'si
  `atlas_validation_test_20260905_1954` ve izole test bucket'ları silindi; bunlar yalnız
  yeniden üretilebilir test fixture verileriydi. Kullanıcı artifact'ı silinmedi.

## Açık sınırlar

Gerçek ADEME package build/intake yapılmadı. Katalog provisioning, gerçek lisans
kanıtı, compound dimension/applicability ve taxonomy/intended-use/formula owner
sözleşmeleri hâlâ bekliyor. Provenance doğrulaması pointer/locator/raw bağını kontrol
eder; bütün alanların kaynak hücrelerden semantik replay sertifikası değildir.
Advisory blocking adaylar review paketinde bulunabilir, publish edilemez. Admin'in
canlı kabul politikası kesinleşmeden teslim yapılmayacak.

Faz başlangıç commit'i `35b82db`; tamamlanan faz mevcut `main` branch'ine commit/push
edilir. Son commit kimliği için git log kullanılmalı; self-referential hash kaydı tutulmaz.
